[package]
name = "app"
version = "0.1.0"
edition = "2021"

[dependencies]
api = { path = "../api", package = "api" }

reqwest = { version = "0.11", features = ["json", "cookies", "blocking"] }

tui = "0.19"
crossterm = "0.25"